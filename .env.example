# Server only. Set in Vercel Production and Preview as appropriate.
# JSON containing project_id, client_email and private_key for the mobile Firebase project.
FIREBASE_SERVICE_ACCOUNT=
# Optional for running the compiled server locally:
PORT=3000
